---
const stats = [
  {
    title: "Présence Digitale",
    value: "85%",
    description: "des consommateurs marocains recherchent des produits et services en ligne avant l'achat"
  },
  {
    title: "Croissance E-commerce",
    value: "+30%",
    description: "de croissance annuelle du marché e-commerce au Maroc"
  },
  {
    title: "Engagement Client",
    value: "20M+",
    description: "d'utilisateurs de réseaux sociaux au Maroc"
  }
]
---

<section class="relative py-24 bg-gradient-to-br from-gray-50 via-white to-blue-50/30">
  <!-- Background decoration -->
  <div class="absolute inset-0">
    <div class="absolute top-0 left-1/4 w-96 h-96 bg-blue-200 rounded-full mix-blend-multiply filter blur-3xl opacity-10 animate-pulse-slow"></div>
    <div class="absolute bottom-0 right-1/4 w-96 h-96 bg-violet-200 rounded-full mix-blend-multiply filter blur-3xl opacity-10 animate-pulse-slow animation-delay-2000"></div>
  </div>

  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-20">
      <h2 class="text-4xl sm:text-5xl font-bold text-gray-900 mb-6 leading-tight">
        L'Importance du 
        <span class="relative inline-block">
          <span class="relative z-10">Marketing Digital</span>
          <div class="absolute -bottom-2 left-0 right-0 h-3 bg-blue-100/50 -rotate-1"></div>
        </span>
        au Maroc
      </h2>
      <p class="text-xl text-gray-600 max-w-3xl mx-auto leading-relaxed">
        Dans un marché marocain en pleine transformation numérique, une stratégie digitale solide est essentielle pour votre succès.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 lg:gap-12">
      {stats.map((stat, index) => (
        <div class="group relative">
          {/* Animated background gradient */}
          <div class="absolute inset-0 bg-gradient-to-br from-primary-500/20 to-primary-600/20 rounded-3xl blur-xl opacity-0 group-hover:opacity-100 transition-all duration-700 scale-90 group-hover:scale-105"></div>
          
          {/* Card content */}
          <div class="relative h-full p-8 sm:p-10 bg-white/80 backdrop-blur-sm rounded-3xl shadow-lg hover:shadow-2xl transition-all duration-500 group-hover:-translate-y-1 border border-gray-100">
            {/* Decorative corner */}
            <div class="absolute top-0 right-0 w-24 h-24 overflow-hidden">
              <div class="absolute top-0 right-0 w-16 h-16 bg-gradient-to-br from-primary-100 to-primary-200 -rotate-12 transform origin-bottom-left"></div>
            </div>

            {/* Number highlight */}
            <div class="relative mb-6">
              <div class="text-5xl font-bold bg-gradient-to-r from-primary-600 to-primary-500 bg-clip-text text-transparent">
                {stat.value}
              </div>
            </div>

            {/* Title with underline effect */}
            <h4 class="text-2xl font-bold text-gray-900 mb-4 relative inline-block">
              {stat.title}
              <div class="absolute bottom-0 left-0 w-full h-0.5 bg-primary-200 transform scale-x-0 group-hover:scale-x-100 transition-transform duration-500"></div>
            </h4>

            {/* Description */}
            <p class="text-lg text-gray-600 leading-relaxed">
              {stat.description}
            </p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
.animate-pulse-slow {
  animation: pulse 4s cubic-bezier(0.4, 0, 0.6, 1) infinite;
}

.animation-delay-2000 {
  animation-delay: 2s;
}

@keyframes pulse {
  0%, 100% {
    opacity: 0.1;
  }
  50% {
    opacity: 0.15;
  }
}
</style>