---
import Layout from '../../layouts/Layout.astro';
import ContactForm from '../../components/ContactForm.astro';
import ServicesGrid from '../../components/ServicesGridCity.astro';
import { cities } from '../../data/cities';

export async function getStaticPaths() {
  const allCities = cities.flatMap(section => section.cities);
  
  return allCities.map(city => ({
    params: { city: city.slug },
    props: { city }
  }));
}

const { city } = Astro.props;
const title = `Agence Marketing Digital à ${city.name} - Agence V5`;
const description = `Votre partenaire marketing digital à ${city.name}. Experts en SEO, réseaux sociaux et publicité en ligne. Solutions personnalisées pour votre entreprise.`;
---

<Layout title={title} description={description}>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-12">
      <!-- Main Content -->
      <div class="lg:col-span-2">
        <h1 class="text-4xl font-bold text-gray-900 mb-6">
          Agence Marketing Digital à {city.name}
        </h1>
        
        <div class="prose prose-lg max-w-none mb-12">
          <div class="bg-gradient-to-br from-primary-50 to-white rounded-2xl p-8 mb-8">
            <h2 class="text-2xl font-semibold text-gray-900 mb-4">
              Votre Partenaire Digital à {city.name}
            </h2>
            <p class="text-gray-600">
              Agence V5 est votre partenaire de confiance en marketing digital à {city.name}. 
              Notre expertise locale et notre approche personnalisée vous garantissent des résultats 
              concrets pour votre entreprise.
            </p>
          </div>

          <section>
            <h2 class="text-2xl font-semibold text-gray-900 mb-4">
              Marketing Digital sur Mesure à {city.name}
            </h2>
            <p>
              Dans un marché local aussi dynamique que {city.name}, une présence digitale forte est 
              essentielle pour se démarquer. Notre équipe d'experts combine une connaissance approfondie 
              du marché de {city.name} avec les meilleures pratiques du marketing digital pour offrir 
              des solutions qui correspondent parfaitement à vos besoins.
            </p>
          </section>

          <section>
            <h2 class="text-2xl font-semibold text-gray-900 mb-4">
              Pourquoi Choisir Agence V5 à {city.name} ?
            </h2>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
              <div class="bg-white rounded-xl shadow-lg p-6">
                <h3 class="text-xl font-semibold text-primary-500 mb-3">Expertise Locale</h3>
                <p class="text-gray-600">
                  Une compréhension approfondie du marché de {city.name} et de ses spécificités.
                </p>
              </div>
              <div class="bg-white rounded-xl shadow-lg p-6">
                <h3 class="text-xl font-semibold text-primary-500 mb-3">Solutions Personnalisées</h3>
                <p class="text-gray-600">
                  Des stratégies adaptées aux besoins uniques de votre entreprise à {city.name}.
                </p>
              </div>
            </div>
          </section>

          <section>
            <h2 class="text-2xl font-semibold text-gray-900 mb-4">
              Notre Impact à {city.name}
            </h2>
            <p>
              Depuis notre établissement, nous avons accompagné de nombreuses entreprises de {city.name} 
              dans leur transformation digitale. Notre approche combine expertise technique, créativité 
              et compréhension approfondie des enjeux locaux pour délivrer des résultats mesurables.
            </p>
            <ul class="space-y-4 mt-4">
              <li class="flex items-start">
                <svg class="w-6 h-6 text-primary-500 mr-3 mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                <span>Stratégies digitales adaptées au marché de {city.name}</span>
              </li>
              <li class="flex items-start">
                <svg class="w-6 h-6 text-primary-500 mr-3 mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                <span>Support local et réactivité maximale</span>
              </li>
              <li class="flex items-start">
                <svg class="w-6 h-6 text-primary-500 mr-3 mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                </svg>
                <span>Résultats mesurables et ROI optimisé</span>
              </li>
            </ul>
          </section>
        </div>

        <!-- Call to Action -->
        <div class="flex flex-col sm:flex-row gap-4 items-center justify-center mb-16">
          <a 
            href="/contactez-nous" 
            class="btn-primary"
          >
            Demander un Devis Gratuit
          </a>
          <a 
            href="tel:+212661196147" 
            class="inline-flex items-center px-6 py-3 border border-primary-500 text-base font-medium rounded-md text-primary-600 bg-white hover:bg-primary-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary-500"
          >
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
            </svg>
            +212 6 61 19 61 47
          </a>
        </div>

        <!-- Services Grid -->
        <ServicesGrid city={city} />
      </div>

      <!-- Sidebar with Contact Form -->
      <div class="lg:col-span-1">
        <div class="sticky top-24">
          <ContactForm />
        </div>
      </div>
    </div>
  </div>
</Layout>